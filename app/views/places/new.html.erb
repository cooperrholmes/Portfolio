<h1> Leave a comment / review! </h1>

<%= simple_form_for @places do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :author %>
  <br />
  <%= f.submit 'Create', class: 'btn btn-primary' %>
<% end %>
